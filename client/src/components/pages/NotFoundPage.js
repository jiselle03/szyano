import React from 'react';

import { Typography } from '@material-ui/core';

const NotFoundPage = () => {
    return (
        <Typography variant="h2" style={{color: "maroon"}}>
            404 Not Found
        </Typography>
    );
};

export default NotFoundPage;
