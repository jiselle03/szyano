import React from 'react';
import { BrowserRouter, Switch } from 'react-router-dom';

const App = () => {
  return (
    <BrowserRouter>
      <header>
      </header>

      <Switch>

      </Switch>
    </BrowserRouter>
  );
};

export default App;
